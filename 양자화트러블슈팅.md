# AIMODE v3 + 양자화 환경 트러블슈팅

**작성일**: 2026-05-11
**환경**: RTX 4060 Laptop 8GB VRAM, Gemma3:12b / Gemma3:4b-it-qat (NF4 양자화 추정)
**대상 파일**:
- `App/backend/routes/aimode.py` — AIMODE LangGraph
- `App/backend/app.py` — prewarm·VRAM 전략
- `App/frontend/src/pages/MainAI.jsx` — Mode 배지·References 패널·답변 인용 chip

---

## TL;DR

1. **8GB VRAM 환경**에서 검색 엔진(SigLIP2+DINOv2+BGE-M3+Reranker ≈ 5GB) + 인덱싱용 Qwen-VL(NF4 1.2GB) + 답변용 Gemma3:4b(~3GB) 동시 적재 불가 → **prewarm 비활성** + **lazy load** + **VRAM swap** 전략으로 우회.
2. **Gemma3:4b-it-qat 가 `<unused344>` 같은 vocab special token 을 평문으로 출력** → 정규식 필터로 차단.
3. **`_save_history` 가 LangGraph state 에 messages 필드 silent drop** → fallback dict 이중 저장으로 보강.
4. **followup_search 가 multi-word phrase 를 PDF substring 매칭 못 함** → 토큰 분해해서 distinct 단어 수로 점수.
5. **목차 페이지가 본문보다 높은 점수** → 점선·페이지 참조 패턴 페널티 + 단위 숫자 보너스.

---

## 그래프 구조 (v3)

```
START → router
  router (조건):
    rag      → intent → [mode 분기]
                          structured → search → scan → select → extract → generate
                          open       → fulltext_search → extract → generate
    followup → followup_intent → followup_search
                                    exist → extract → generate
                                    none  → fallback_marker → intent (RAG 사이클)
  → generate → END
```

**v1 → v3 변경 요약**

| 항목 | v1 | v3 |
|---|---|---|
| router 분기 | rag / chat / followup / qa_gen | **rag / followup 2개만** |
| 새 채팅 첫 메시지 | LLM 자동 분류 | **history empty → RAG 강제** |
| chat / qa_gen 노드 | 활성 | **제거** (코드는 남기되 그래프에서 빠짐) |
| RAG intent | 키워드만 추출 | **mode 분류 추가** (structured/open) |
| **open mode** | (없음) | **신규** — 모든 색인 페이지 캐시 substring 매칭, vector 검색 우회 |
| followup | 이전 파일 그대로 generate | **새 키워드로 이전 source 본문 재검색** → 매칭 시 generate, 미매칭 시 RAG 사이클 |
| **extract 노드** | (없음) | **신규** — chunks_meta → references (page/timestamp 메타) |
| 답변 인용 | 시스템 프롬프트만 | **시스템 프롬프트 강제 + 프론트 chip 변환** |

---

## 1. VRAM 8GB 트러블슈팅

### 1.1 증상

```
[+0s]   POST /api/index/start
[+2s]   step 1 "문서 페이지 렌더링 중"
[+30s]  step 2 "Qwen2-VL 캡션 생성 중 (103페이지)"
[+무한] keepalive만 도달, 진행률 0% 고정
```

GPU 사용률 0%, 전용 VRAM 5.9/8.0 GB 점유 상태. inference 자체가 시작 안 됨.

### 1.2 원인

- 백엔드 부팅 시 `_warmup_engine` 이 **SigLIP2+DINOv2+BGE-M3+CLAP** 4종 prewarm → ~5GB VRAM 점유
- 인덱싱 시작 시 Qwen2-VL-2B (NF4 ~1.2GB) 추가 로드
- 합쳐서 6~7GB 가 차면 PyTorch CUDA allocator 의 fragment 문제로 **첫 inference 가 deadlock**
- 부팅 로그에 경고: `expandable_segments not supported on this platform` — RTX 4060 Laptop 이 modern allocator 미지원

### 1.3 격리 테스트로 진단

```powershell
python -c @"
import sys, time
sys.path.insert(0, r'C:\Honey\DB_insight\DI_TriCHEF')
from captioner.qwen_vl_ko import QwenKoCaptioner
from PIL import Image
import torch

print(f'[0] CUDA mem before: {torch.cuda.memory_allocated()/1e9:.2f} GB')
cap = QwenKoCaptioner(quantize='nf4')
cap._load()
print(f'[1] CUDA mem after load: {torch.cuda.memory_allocated()/1e9:.2f} GB')

img = Image.new('RGB', (672, 672), color=(50, 100, 150))
result = cap.caption(img)
print(f'[2] 결과: {result!r}')
"@
```

**결과**: 단독 26초 로드 + 6.4초 추론 정상 — **Qwen-VL 자체는 정상**. 다른 모델과 동시 적재 시 hang 확정.

### 1.4 처방 — prewarm 비활성

`App/backend/app.py`:

```python
# 부팅 시 검색 엔진 자동 prewarm 비활성. 첫 검색 lazy load 6~15s 한 번 페널티 수용.
# 명시적 prewarm 필요시 OMC_FORCE_WARMUP=1 환경변수로 부활 가능.
if os.environ.get("OMC_FORCE_WARMUP", "").strip() == "1":
    # 기존 prewarm 코드 (default 비활성)
    ...
else:
    _warmup_progress["stage"] = "skipped"
    _warmup_progress["done"] = True
    _warmup_event.set()
```

CLAP prewarm 도 같은 방식으로 토글.

### 1.5 트레이드오프

| 항목 | prewarm ON (24GB) | prewarm OFF (8GB) |
|---|---|---|
| 부팅 후 첫 검색 | 1초 미만 | **6~15초 (cold load)** |
| 부팅 직후 VRAM | ~5GB 점유 | 0GB |
| 인덱싱과 충돌 | hang 위험 | 안전 |
| 매번 검색 | 1초 미만 (캐시) | 1초 미만 (캐시) |

8GB 환경에선 prewarm OFF 가 유일한 안전 선택.

### 1.6 VRAM swap 로직 (generate_node)

검색 모델이 점유 중인 상태에서 LLM 답변 시작할 때:

```python
if _t.cuda.is_available():
    _free_mb = int(_t.cuda.mem_get_info()[0] / 1024 / 1024)
    if _free_mb < 4000:
        _emit({"type": "info", "message": "GPU 메모리 확보 중...", "free_mb": _free_mb})
        _release_search_embedders()
        # Ollama keep_alive=0 으로 강제 unload → 다음 호출에서 GPU 재로드
        _req.post(f"{OLLAMA_URL}/api/generate",
                  json={"model": gen_model, "keep_alive": 0, "stream": False},
                  timeout=10)
```

**부작용**: VRAM 스왑 자체가 ~140초 hang 유발 가능 (Ollama 재로드 + cuDNN 컴파일). 첫 답변이 매우 느림.

---

## 2. Gemma3 Special Token 노출

### 2.1 증상

답변 본문에 vocab 의 unused/제어 토큰이 평문 그대로 출력:

```
<unused344>년 세계 CRM 시장은 전년대비 4.6% 성장했습니다.
```

또는 다른 언어 토큰 hallucination:
- `હવામાન` (Gujarati 어 "날씨")
- `泄漏率` (중국어 "누설율")
- `月中` (한자)

### 2.2 원인

- Gemma3:4b-it-qat 의 **NF4 양자화 + multilingual vocab** 의 known issue
- 모델이 토큰 생성 중 vocab 의 `<unused0>`~`<unused...>` 같은 sentinel token 을 무작위로 선택
- Ollama 가 디코딩 시 special token 처리 옵션 없음 → 평문으로 출력

### 2.3 처방 — 클라이언트 측 정규식 필터

`_ollama_stream` 과 `_ollama_oneshot` 의 모든 yield 직전에 적용:

```python
import re as _re_spec
_GEMMA_SPECIAL_RE = _re_spec.compile(
    r"<(?:unused\d+|pad|eos|bos|/s|s|end_of_turn|start_of_turn|im_start|im_end)>",
    _re_spec.IGNORECASE,
)

def _strip_special_tokens(text: str) -> str:
    if not text:
        return text
    return _GEMMA_SPECIAL_RE.sub("", text)
```

stream 처리:

```python
buf += tok
if "\n" in buf or len(buf) >= chunk_size:
    cleaned = _strip_special_tokens(buf)
    if cleaned:
        yield cleaned
    buf = ""
```

### 2.4 한계 — 다국어 토큰 환각은 별건

`હવામાન`, `泄漏率` 같은 일반 텍스트 토큰은 special token 정규식으로 안 잡힘. 시스템 프롬프트에 "한국어로만 답변" 강제로 어느 정도 완화. 근본 해결은 답변 모델을 더 큰 모델(12b)로 변경.

---

## 3. History 이중 저장 문제

### 3.1 증상

후속 질문 시 router 가 followup 분류하지 않고 RAG 강제로 빠짐:

```
1차: "SW산업 수출액은 얼마야?" → 정상
2차 (같은 thread_id): "전년대비 얼마나 상승한거야?"
   → route: rag (followup 기대)
   → history empty 로 판단됨
```

### 3.2 원인

`_save_history` 가 LangGraph store 에 update_state 호출하고 성공으로 보고:

```python
def _save_history(thread_id, question, answer):
    g = _get_history_graph()
    if g is not None and _LANGGRAPH_OK:
        try:
            g.update_state(cfg, {..., "messages": [...]})
            return   # ← 성공으로 보고 return → fallback dict 안 거침
        except Exception:
            ...
    # fallback dict 저장 (위에서 return 되면 여기까지 안 옴)
```

문제: **`RAGState` TypedDict 에 `messages` 필드가 없음**. LangGraph 가 update_state 호출 시 `messages` 만 silent drop 하고 다른 필드만 저장 → return → fallback 안 거침.

`_load_history` 가 다음 호출 시 LangGraph 에서 messages 비어있음을 받고 → fallback 도 비어있음 → return [].

### 3.3 처방 — 이중 저장 + fallback 우선

```python
def _save_history(thread_id, question, answer):
    # 1) LangGraph store 시도 (실패해도 무시)
    g = _get_history_graph()
    if g is not None and _LANGGRAPH_OK:
        try:
            g.update_state(...)
        except Exception:
            pass
    # 2) 항상 fallback dict 에도 저장 (이중 보장)
    with _fallback_lock:
        h = _fallback_history.setdefault(thread_id, [])
        h.append({"role": "user", "content": question})
        h.append({"role": "assistant", "content": answer})


def _load_history(thread_id):
    g = _get_history_graph()
    if g is not None:
        try:
            st = g.get_state(cfg)
            if st and st.values:
                msgs = st.values.get("messages") or []
                if msgs:
                    return [...]
        except Exception:
            pass
    # LangGraph 비어있거나 실패 → fallback dict
    with _fallback_lock:
        return list(_fallback_history.get(thread_id, []))
```

이후 followup 라우트 정상 작동 확인.

---

## 4. Followup Search 매칭 실패

### 4.1 증상

```
1차 답변: "SW산업 수출액은 2015년 92억 달러였다."
2차 query: "전년대비 얼마나 상승한거야?"
2차 router: followup ✓
2차 intent: mode=followup, kw=["SW산업 수출액 전년대비", "수출액 증감"]
2차 followup_search_result: matched=0  ← ⚠️
2차 followup_fallback → RAG 사이클 → CRM 4.6% 오답
```

### 4.2 원인

`followup_intent_node` 의 LLM prompt 보강 (이전 답변 주제어 + 현재 질문 결합) 으로 키워드가 정확히 추출됨:
- ✅ `["SW산업 수출액 전년대비", "수출액 증감"]`

그러나 `followup_search_node` 가 PDF 본문에서 **multi-word phrase 그대로 substring 매칭** 시도:
- "SW산업 수출액 전년대비" 가 PDF 에 그대로 안 나타남 (PDF: "SW산업의 수출액은 2015년...")
- substring 매칭 실패 → found=False → 사이클 폴백

### 4.3 처방 — phrase 토큰 분해

```python
# followup_search_node 안에서 detail_kws 확장
_STOP_TOKENS = {"의","는","은","이","가","을","를","와","과","에",
                "정의","근거","원인","변화","추이","현황"}
expanded = []
seen = set()
for kw in detail_kws:
    if kw and kw.lower() not in seen:
        seen.add(kw.lower())
        expanded.append(kw)
    if " " in kw:
        for w in kw.split():
            wl = w.lower()
            if len(w) >= 2 and wl not in seen and w not in _STOP_TOKENS:
                seen.add(wl)
                expanded.append(w)
```

결과:
```
원본: ["SW산업 수출액 전년대비", "수출액 증감"]
   ↓
확장: ["SW산업 수출액 전년대비", "SW산업", "수출액", "전년대비", "수출액 증감", "증감"]
```

`_scan_pdf_pages` 가 distinct 매칭 단어 수로 점수 매김 → SW산업 본문 페이지가 1순위로 잡힘.

---

## 5. Open Mode 페이지 점수 매기기 (목차 vs 본문)

### 5.1 증상

"DBMS 시장 세계 전망" query → 답변: "정보가 없습니다" (오답)

실제 PDF 에 DBMS 시장 본문 (60페이지 근처: "300억 달러 돌파, 6.4% 성장") 존재함에도 못 찾음.

### 5.2 원인

`fulltext_search_node` 가 페이지마다 substring 매칭 + score 매김. **목차 페이지가 본문보다 높은 점수**:

| 페이지 | 내용 | distinct 매칭 키워드 | 점수 |
|---|---|---|---|
| 6 (목차) | "...DBMS 시장... 60 보안 SW 시장... 65..." | 3 (DBMS, 시장, 세계) | 15+ |
| 40 (본문) | "...2015년 시장 규모는 5,473억 원..." | 1 (시장) | 5 |

목차 페이지에 챕터 제목이 다 들어있어서 distinct 키워드 매칭이 압도적.

### 5.3 처방 — 목차 페널티 + 본문 보너스

```python
toc_dots = text.count("…")
toc_page_refs = len(re.findall(r"…+\s*\d{1,3}\b", text))
is_toc_strong = (toc_page_refs >= 5) or (toc_dots >= 30)

if is_toc_strong:
    score = (hits + distinct + min(unit_nums, 5)) // 2 - toc_page_refs
else:
    score = hits * 2 + distinct * 5 + min(unit_nums, 5) * 3 - toc_dots // 3
```

추가로 `extract_node` 에 **목차 references 자동 제외** 헬퍼:

```python
def _looks_like_toc_snippet(text: str) -> bool:
    if text.count("…") >= 5:
        return True
    if len(re.findall(r"…+\s*\d{1,3}\b", text)) >= 3:
        return True
    lines = [ln.strip() for ln in text.split("\n") if ln.strip()]
    short_lines = sum(1 for ln in lines if len(ln) <= 30)
    page_ending = sum(1 for ln in lines if re.search(r"\d{1,3}\s*$", ln))
    if len(lines) >= 4 and short_lines / len(lines) > 0.6 and page_ending / len(lines) > 0.5:
        return True
    return False
```

### 5.4 결과

DBMS 케이스 답변:
```
2016년 국내 DBMS 시장 규모는 5,782억 원으로 예상되며,
2014년에는 0.8% 성장하는데 그쳤으나,
2015년 3.1%, 2016년 5.7%로 시장 성장률이 점차 증가할 전망입니다.
```

**본문 수치 정확 인용** — 목차 노이즈 사라짐. 답변 환각 0.

---

## 6. UI — Mode 배지 + References 패널 + 답변 인용 chip

### 6.1 추가된 UI 요소

**(a) Mode 배지** — "질문 의도 분석" 카드 우측:
- 🔍 키워드 검색 (open) — emerald
- 📂 구조 검색 (structured) — sky
- ↩ 후속 질문 (followup) — amber

**(b) References 패널** — 답변 카드 **아래** (옵션 A):
- 페이지/타임스탬프 chip + 파일명 + snippet 미리보기
- 클릭 시 PDF detail 열기
- 답변 있을 때만 표시 (`aimodeAnswer && aimodeReferences.length > 0`)

**(c) 답변 본문 인용 chip**:
- 답변 텍스트의 `(XX페이지)`, `[MM:SS]` 자동 감지 → 클릭 가능한 chip 으로 변환
- references 매칭 시 해당 source 로 점프

### 6.2 시스템 프롬프트 인용 강제

`_build_rag_messages` 에 references 블록 추가:

```
[원본 위치 정보 — 답변에서 출처 표시 시 아래 태그 그대로 사용]
  (40페이지) 2015_SW산업 연간보고서.pdf: ... 5,473억 원...
  (48페이지) 2015_SW산업 연간보고서.pdf: ... SCM 시장 95억 달러...
사용 가능한 태그: (40페이지), (48페이지), ...

[인용 형식 — 반드시 준수]
- 문서 인용 시: 각 문장 끝에 반드시 (XX페이지) 형식으로 페이지 표시. 누락 금지.
- 비디오/오디오: [MM:SS] 또는 [HH:MM:SS] 타임스탬프 표기 필수.
- 새 페이지·시각 만들면 안 됨.

[답변 형식 예시]
1. DBMS 시장은 2016년 317억 달러로 6.4% 성장 (40페이지)
2. 2019년 384억 달러 규모 전망 (40페이지)
```

### 6.3 한계

Gemma3:4b-it-qat 가 시스템 프롬프트 인용 형식 일관성 있게 따르지 못함. 답변에 `(XX페이지)` 가 나타날 때도 있고 누락될 때도 있음. 12b 로 변경하면 instruction-following 더 강하지만 8GB 환경에선 VRAM 트레이드오프.

---

## 7. 검증 시나리오

### 7.1 시나리오 1: open mode (단일 키워드)

```js
// query: "DBMS 시장 세계 전망"
// 기대: route=rag, mode=open, candidates=SW산업 보고서 only,
//       references 본문 페이지 위주, 답변 본문 수치 인용
```

### 7.2 시나리오 2: structured mode

```js
// query: "SW산업 보고서에서 취업유발효과 찾아줘"
// 기대: route=rag, mode=structured, search → scan → select → extract → generate
```

### 7.3 시나리오 3: followup → exist

```js
// 1차: "SW산업 수출액은 얼마야?" → "92억 달러"
// 2차 (같은 thread_id): "전년대비 얼마나 상승한거야?"
// 기대: route=followup, intent kw=["SW산업 수출액 전년대비", ...],
//       followup_search_result matched ≥ 1, generate → "7.9%" 정답
```

### 7.4 시나리오 4: followup → none → RAG 사이클

```js
// 1차: "DBMS 시장 전망" → SW산업 보고서 답변
// 2차: "양자컴퓨터 동향은?" (이전 source 에 없는 주제)
// 기대: route=followup → followup_search matched=0 → followup_fallback
//       → fallback_marker → intent (RAG) → 전체 색인 검색
```

---

## 8. 알려진 한계 / 미해결

| 항목 | 상태 | 비고 |
|---|---|---|
| 다국어 토큰 환각 (Gujarati/중국어) | 미해결 | Gemma3:4b multilingual vocab 한계. 12b 변경 검토 |
| 답변에 페이지 인용 chip 일관성 | 불안정 | 시스템 프롬프트 보강했으나 4b 가 무시 빈번 |
| VRAM swap 후 첫 토큰 ~140s | 미해결 | Ollama 재로드 + cuDNN 컴파일 비용 |
| 답변 본문 ↔ references 자동 매핑 | 미구현 | 옵션 B (줄별 자동 페이지 표시) 다음 라운드 |
| AIMODE 좌측 사이드바 채팅방 UI | 미구현 | 별건 안건 |
| 프론트 후속 질문 thread_id 유지 | 미구현 | 현재 fetch 호출 시 thread_id 명시 필요 |

---

## 9. 핵심 교훈

1. **8GB VRAM 환경에선 모델을 "필요할 때만 로드"** 가 유일한 안전 전략. 부팅 시 prewarm 은 24GB 가정.
2. **PyTorch CUDA allocator fragmentation** 는 RTX 4060 Laptop 같은 모바일 GPU 에서 빈번. `expandable_segments not supported` 경고가 떴다면 메모리 swap 로직 필수.
3. **NF4 양자화 모델은 vocab 의 special token 을 평문으로 출력하는 케이스가 있음** → 클라이언트 측 정규식 필터 필수.
4. **LangGraph TypedDict 의 필드 누락은 silent drop 됨** → 핵심 데이터는 fallback store 이중 저장.
5. **목차 페이지는 키워드 밀도가 본문보다 압도적으로 높음** — 단순 keyword density score 로는 본문 못 잡음. 점선·페이지 참조·단위 숫자 같은 구조적 신호 활용 필수.
6. **Multi-word phrase 매칭은 PDF substring 검색에 거의 안 맞음** — 토큰 분해 + distinct 매칭 단어 수로 점수가 robust.
7. **4b 양자화 모델은 instruction-following 약함** — 시스템 프롬프트 규칙을 무시하거나 다국어 토큰 환각. 12b 가 답.

---

## 10. 변경된 코드 위치 빠른 참조

| 변경 항목 | 파일 | 위치 |
|---|---|---|
| Special token 필터 | `aimode.py` | `_GEMMA_SPECIAL_RE`, `_strip_special_tokens`, `_ollama_stream`, `_ollama_oneshot` |
| router 단순화 (2-route) | `aimode.py` | `router_node` |
| RAG mode 분류 | `aimode.py` | `_classify_query_mode`, `intent_node` |
| open mode | `aimode.py` | `fulltext_search_node`, `_after_intent_edge` |
| scan_pdf_pages (페이지 단위) | `aimode.py` | `_scan_pdf_pages`, `_scan_one`, `scan_node` |
| 목차 점수 페널티 | `aimode.py` | `fulltext_search_node` 점수 계산 |
| extract 노드 | `aimode.py` | `extract_node`, `_looks_like_toc_snippet` |
| followup_intent 보강 | `aimode.py` | `followup_intent_node` prompt |
| followup_search 토큰 분해 | `aimode.py` | `followup_search_node` |
| history 이중 저장 | `aimode.py` | `_save_history`, `_load_history` |
| 답변 인용 시스템 프롬프트 | `aimode.py` | `_build_rag_messages` (references 블록 + 인용 강제) |
| prewarm 비활성 토글 | `app.py` | `_warmup_engine` 호출부 (OMC_FORCE_WARMUP) |
| Mode 배지 UI | `MainAI.jsx` | "질문 의도 분석" 카드 |
| References 패널 (답변 아래) | `MainAI.jsx` | 답변 카드 다음 div |
| 답변 본문 인용 chip | `MainAI.jsx` | 답변 카드 본문 텍스트 렌더 |
| extract SSE 핸들러 | `MainAI.jsx` | `case "extract"` |
