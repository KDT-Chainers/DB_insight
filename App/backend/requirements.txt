Flask
flask-cors

# 벡터 저장소
chromadb>=0.6.0

# 텍스트 임베딩 (공통)
sentence-transformers

# 동영상 임베딩 (M11) ✅ 구현 완료
faster-whisper        # Whisper medium STT (한국어)
transformers          # BLIP 이미지 캡셔닝
opencv-python         # 프레임 추출
Pillow                # 이미지 처리
torch                 # BLIP/Whisper GPU 지원
numpy

# 문서 추출 (doc.py 구현 시 필요한 것만 설치)
# pdfplumber         # PDF
# python-docx        # DOCX
# python-pptx        # PPTX
# openpyxl           # XLSX

# --- TRI-CHEF 추가 의존성 ---
open-clip-torch>=2.24          # SigLIP2 로더
timm>=1.0                      # DINOv2-large 래퍼
pdfminer.six                   # PDF 텍스트 + 좌표
pymupdf>=1.24                  # PDF → JPEG 페이지 렌더 (PyMuPDF)
python-docx                    # DOCX 텍스트/이미지
openpyxl                       # XLSX
olefile                        # HWP 기초 파서
langgraph>=0.2                 # LangGraph 12-node
scipy                          # 통계 calibration (선택)
tqdm                           # 진행률
accelerate                     # HF 모델 device_map
