# 🎤 AIMODE LangGraph 발표 대본

> **총 길이**: 약 5~6분
> **슬라이드**: 2장 (왜 LangGraph 인가 / 그래프가 작동하는 법)
> **옵션**: A — Why ↔ How 구조

---

## 📄 슬라이드 1 — 왜 LangGraph 인가 (약 2분 30초)

### 🟣 오프닝 (10초)

> "지금부터 AIMODE 가 왜 LangGraph 위에서 돌아가는지 말씀드리겠습니다.
> 이 슬라이드는 **'왜 굳이 그래프 자료구조까지 동원해야 했는지'** — 그 이유를 다룹니다."

---

### 🟦 [좌측 카드 — "일반모드 vs AIMODE"] (40초)

> "먼저 문제정의부터.
>
> 우리 시스템에는 두 가지 모드가 있습니다. 일반모드와 AIMODE.
>
> [일반모드 카드 가리키며]
> **일반모드**는 흔히 생각하는 검색입니다. '이 파일 어디 있어?' 를 묻고, 결과로 **파일 카드 리스트**를 받죠. 사용자가 직접 그 파일을 열어서 안에서 답을 찾습니다.
>
> [AIMODE 카드 가리키며]
> 반면 **AIMODE**는 다릅니다. 사용자가 묻는 건 '결론이 뭐야?' 입니다.
> 결과는 파일이 아니라 **이미 완성된 한국어 답변** 입니다. 시스템이 문서 여러 페이지를 직접 읽고, 모으고, 합성해서 한 문단으로 만들어줘야 하죠.
>
> 한 마디로 AIMODE 는 **검색이 아니라 합성(synthesis)** 입니다. 그래서 단순한 함수 한 줄로는 풀리지 않습니다."

---

### 🟥 [중앙 카드 — "Naive RAG 한계"] (50초)

> "그럼 RAG 의 가장 기본형 — 흔히 말하는 **Naive RAG** 로 풀면 안 될까?
>
> [상단의 직선 다이어그램 가리키며]
> 교과서적인 흐름은 이렇습니다. **질문 → 벡터검색 → 컨텍스트 합치기 → LLM 한 방 → 답**.
> 직선이죠. 분기도 없고 사이클도 없고.
>
> [6개 미니 카드 차례로 가리키며]
> 실제로 돌려보면 6가지 한계가 바로 드러납니다.
>
> 첫째, **분기가 없습니다**. '안녕' 같은 잡담에도 PDF 를 뒤집니다.
> 둘째, **후속 질문이 안 됩니다**. '전년 대비는?' 같은 질문도 처음부터 다시 검색해서 직전 맥락이 다 날아갑니다.
> 셋째, **페이지 단위 인용이 불가능**합니다. '40페이지에 있다' 같은 정확한 위치를 줄 수 없죠.
> 넷째, **진행 표시가 없습니다**. 사용자는 그저 스피너만 보고 기다립니다.
> 다섯째, **폴백이 없어서** 매칭이 약해도 1순위 문서를 강제로 답하는 환각이 나옵니다.
> 마지막, **상태 보관이 어렵습니다**. history, sources, references 를 매 함수마다 인자로 들고 다니니 시그니처가 폭발합니다.
>
> 결국 이 6가지는 모두 한 문제로 수렴합니다 — **'그래프 자료구조'의 부재**."

---

### 🟢 [우측 카드 — "LangGraph 4 도구"] (45초)

> "그래서 LangGraph 입니다.
> Naive RAG 가 못 풀던 6가지를 LangGraph 는 정확히 **4가지 도구**로 풀어줍니다.
>
> [① StateGraph 미니 가리키며]
> 첫째, **StateGraph**. 모든 노드가 `RAGState` 라는 dict 하나를 공유합니다. 함수 인자가 사실상 0개죠. 노드는 일부 필드만 update 한 dict 를 반환하면 됩니다.
>
> [② 조건부 엣지]
> 둘째, **조건부 엣지**. `add_conditional_edges` 한 줄로 분기를 정의합니다. 라우터든 intent 든 followup 이든 다음 노드를 동적으로 선택할 수 있죠.
>
> [③ MemorySaver]
> 셋째, **MemorySaver**. `thread_id` 만 같으면 LangGraph 가 직전 state 를 자동 복원합니다. 후속 질문에서 직전 sources 가 그대로 살아 있는 비결입니다.
>
> [④ 노드 단위 SSE]
> 넷째, **노드 단위 SSE**. 각 노드가 진입·진행·완료 시점에 이벤트를 push 합니다. 프론트엔드는 단계별 카드를 실시간으로 그려주죠. 사용자가 '지금 뭐 하고 있는지' 항상 알 수 있게요."

---

### 🔚 [전환] (5초)

> "자, 그러면 이 네 도구가 실제로 어떻게 결합돼서 돌아가는지 — 다음 슬라이드에서 직접 보시죠."

---

## 📄 슬라이드 2 — 그래프가 작동하는 법 (약 2분 30초)

### 🟣 [좌측 — 그래프 SVG 가리키며] (40초)

> "이게 AIMODE LangGraph 의 전체 모습입니다. 총 **10개 노드 + 1개 폴백 마커**.
>
> [START → router 경로]
> 모든 질문은 START 에서 들어와 **router** 가 첫 판단을 합니다. RAG 인지 FOLLOWUP 인지를 결정하죠.
>
> [rag → intent → struct/open]
> RAG 경로로 가면 **intent** 가 받아서 다시 structured / open 으로 갈라집니다. 'X 의 Y 얼마야?' 같은 좁은 질문은 structured 로, '트렌드 알려줘' 같은 광범위한 질문은 open 으로.
>
> [structured 경로 가리키며]
> structured 는 search → scan → select 세 노드를 거치는 정밀 경로입니다.
> [open 경로]
> open 은 fulltext_search 한 노드로 페이지 전수 매칭.
> [followup 경로]
> followup 은 followup_intent 와 followup_search 두 노드로 직전 sources 를 재활용합니다.
>
> 결국 모든 경로는 **extract → generate → END** 로 수렴합니다."

---

### 🟦 [우상단 — 1턴 시나리오 stripe] (60초)

> "실제 질문 하나로 따라가 봅시다.
> 사용자가 **'DBMS 시장 규모 얼마야?'** 라고 묻습니다.
>
> [노드 1 router]
> **router** 는 history 가 비어 있으니 followup 이 될 수 없죠. 즉시 RAG 으로 결정합니다.
>
> [노드 2 intent]
> **intent** 가 받아서 mode 를 structured 로 분류하고, 키워드를 두 계층으로 뽑습니다. file_keywords 는 'DBMS', detail_keywords 는 '시장 규모'.
>
> [노드 3 search]
> **search** 가 BGE-M3 임베딩으로 ChromaDB 에서 top-5 후보 파일을 가져옵니다.
>
> [노드 4 scan]
> **scan** 이 가장 중요한데, 각 후보 PDF 의 페이지를 한 장씩 열어서 키워드 매칭을 합니다. '시장 규모' 가 들어간 페이지에 가산점을 주고, 목차스러운 페이지는 페널티를 줍니다. 그래서 40페이지를 찾아냅니다.
>
> [노드 5 select]
> **select** 가 confidence gate 역할을 합니다. 점수가 컷오프 이하면 답을 안 합니다. Naive RAG 처럼 1순위를 강제로 답하지 않아요.
>
> [노드 6 extract]
> **extract** 가 page text 에서 인용할 부분을 잘라내고 trichef_id · page · snippet 으로 정규화합니다. 프론트엔드는 이걸 받아서 즉시 출처 chip 을 답변 위에 띄웁니다.
>
> [노드 7 generate]
> 마지막으로 **generate** 가 Ollama 로 마크다운 답변을 토큰 단위로 스트리밍합니다. 끝나면 SQLite 에 turn 을 영속해서 다음 후속에 대비하죠."

---

### 🟪 [우하단 좌측 — 2턴 followup stripe] (35초)

> "이제 후속 질문 — **'전년 대비는?'**
>
> [router]
> **router** 가 다르게 판단합니다. history 가 채워져 있고 '전년 대비' 라는 비교어가 보이니, LLM 분류기에 한 줄 물어보고 followup 으로 결정합니다.
>
> [followup_intent]
> **followup_intent** 가 직전 turn 의 sources — 그러니까 2016 IT 백서 40페이지 — 를 회수합니다.
>
> [followup_search]
> **followup_search** 는 그 페이지 캐시 안에서만 다시 스캔합니다. 새로 벡터검색을 돌리지 않으니 훨씬 빠르고 맥락도 그대로죠.
>
> 매칭이 잡히면 **extract → generate** 는 1턴과 똑같이 재사용됩니다. 같은 노드, 다른 경로 — 이게 그래프의 장점입니다."

---

### 🔻 [우하단 우측 — 폴백 + PDF 미니 카드] (25초)

> "[폴백 카드 가리키며]
> 만약 followup 이라 생각했는데 막상 매칭이 0개라면 — **_fallback_marker** 가 작동해서 RAG 사이클로 처음부터 다시 갑니다. state flag 로 무한 루프는 자동 차단되고요.
>
> [PDF 카드]
> 부록으로, 대화가 일단락되면 입력창의 PDF 버튼 한 번으로 LLM 이 전체 대화를 매거진 스타일 PDF 로 정리해줍니다. 이건 그래프 자체와는 독립된 downstream 기능입니다."

---

### 🎬 마무리 (10초)

> "정리하면, AIMODE 는 **합성** 의 영역이고,
> 합성에는 **분기·상태·진행 표시·폴백** 이 필수입니다.
> LangGraph 의 네 도구가 정확히 그 네 가지를 풀어주기 때문에 — AIMODE 가 이 그래프 위에서 돌아갑니다.
>
> 감사합니다."

---

## ⏱️ 시간 분배 가이드

| 슬라이드 | 섹션 | 시간 |
|---|---|---|
| 1 | 오프닝 | 10s |
| 1 | 일반 vs AIMODE | 40s |
| 1 | Naive RAG 한계 | 50s |
| 1 | LangGraph 4 도구 | 45s |
| 1 | 전환 | 5s |
| 2 | 전체 그래프 | 40s |
| 2 | 1턴 시나리오 | 60s |
| 2 | 2턴 followup | 35s |
| 2 | 폴백 + PDF | 25s |
| 2 | 마무리 | 10s |
| **합계** | | **약 5분 20초** |

빨리 말하면 4분 30초, 천천히 가면 6분 — Q&A 까지 합쳐 7~8분짜리 발표에 맞게 짜둔 분량입니다.

---

## 🎯 발표 팁

- **포인터 활용**: 슬라이드 안 여러 영역을 가리키도록 짜여 있으므로, 레이저 포인터나 마우스 커서로 동선을 분명히
- **숫자는 천천히**: "317억 달러", "전년 대비 6.4%" 같은 수치는 한 박자 쉬고 강조
- **전환부**: 슬라이드 1 마지막 "다음 슬라이드에서 직접 보시죠" 부분에서 한 박자 쉬고 슬라이드 넘기기
- **Q&A 대비**:
  - "LangChain 으로는 안 되나요?" → LCEL 은 선형 체인이라 조건부 분기가 hack 필요
  - "왜 페이지 단위인가요?" → 벡터 점수만으로는 정답 페이지 위치를 못 줌, 인용의 신뢰성
  - "환각은 어떻게 막나요?" → select 노드의 confidence gate + system prompt 한국어 강제
